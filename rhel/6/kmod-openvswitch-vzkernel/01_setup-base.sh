#!/bin/bash

set -e

. ./_common.sh

###
#[ -f name-${openvswitch_version}.tar.gz ] || \
# curl -O http://ftp.exmaple.com/name/x.y/name-${openvswitch_version}.tar.gz

#[ -d name-${openvswitch_version} ] || tar zxvf name-${openvswitch_version}.tar.gz
#cd name-${openvswitch_version}
#[ -f Makefile ] || ./configure
