#!/bin/sh

. ./_common.sh

###
#[ -f name-${name_version}.tar.gz ] || \
# curl -O http://ftp.exmaple.com/name/x.y/name-${name_version}.tar.gz

#[ -d name-${name_version} ] || tar zxvf name-${name_version}.tar.gz
#cd name-${name_version}
#[ -f Makefile ] || ./configure
